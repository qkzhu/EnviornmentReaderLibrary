platform :ios, '8.0'


def available_pods
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
   use_frameworks!

    pod 'AFNetworking', '~> 3.0'
end


target 'EnvironmentReaderLibrary' do
    available_pods
end
